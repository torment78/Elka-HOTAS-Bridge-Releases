# Technical Debt Register

This register is the canonical place for HOTASBridge architecture findings and future cleanup candidates. Items should be updated, closed, or split as work proceeds.

| ID | Description | Priority | Risk | Estimated Effort | Dependencies | Status |
| --- | --- | --- | --- | --- | --- | --- |
| TD-001 | Introduce a dependency injection container and move manual service construction out of `App.xaml.cs`. | High | Medium | Medium | Current composition root documentation | Completed foundation; application graph registered through `ApplicationComposition` and resolved with Microsoft DI |
| TD-002 | Split `MainViewModel` responsibilities into runtime coordinator, profile coordinator, device coordinator, and page-specific view models. | High | High | Large | Additional regression tests around UI/runtime behavior | Completed foundation; Core profile/device/input/runtime coordinators and focused page/presentation view models own subsystem behavior. `MainViewModel` intentionally retains application lifecycle, Dispatcher sampling, composition, cross-page reactions, and temporary XAML compatibility proxies; further view/proxy modularization is tracked by TD-010 |
| TD-003 | Complete full `RuntimeSignal` adoption across retained output, recording, playback, and compatibility boundaries. Scripting now consumes immutable signal snapshots. | High | Medium | Medium | RuntimeSignal foundation | Completed; production and regression paths use `ProcessSignal` and immutable `OutputAction` batches, with `InputEvent` retained only inside provider adapters and `XboxState` retained only as backend state |
| TD-004 | Introduce `OutputAction` and output manager/plugin boundary while preserving `XboxState` output. | High | Medium | Medium | RuntimeSignals, output manager chapter | Completed foundation in Chapter 7 |
| TD-005 | Add scheduler/runtime loop abstraction so mapping execution is not tied to UI dispatcher callbacks. | High | Medium | Medium | Runtime metrics and mapping regression tests | Completed foundation in Chapter 12 |
| TD-006 | Expand telemetry into exporter/profiler workflows and long-running history storage. | High | Low | Medium | Telemetry foundation | Completed foundation; shared versioned sessions, analysis, atomic history, and Debug profiler workflows are active |
| TD-007 | Add automated or semi-automated WPF smoke testing that can bypass the first-run driver prompt. | Medium | Medium | Medium | Developer/test launch setting | Completed in Chapter 22 with isolated fresh/existing/Safe Mode startup smoke |
| TD-008 | Add test coverage around HID value-cap parsing and signed logical value normalization. | High | Medium | Medium | Extract HID parsing helpers from native reader | Completed; shared native layout/parser is used by discovery and live input with six focused regressions |
| TD-009 | Add hardware validation checklist and structured compatibility report format. | High | Low | Small | Current feature matrix | Completed in Chapter 14 |
| TD-010 | Modularize `MainWindow.xaml` into page/user controls after behavior is protected. | Medium | Medium | Large | UI smoke tests or screenshot validation | Completed; all primary pages are focused views and unreachable legacy shell blocks were removed |
| TD-011 | Add profile schema tests for backward compatibility across future migrations. | Medium | Medium | Medium | Sample historical profiles | Completed in Chapter 6 |
| TD-012 | Add log retention and async/batched logging to avoid synchronous file writes on hot paths. | Medium | Low | Small | Diagnostics service | Completed; bounded batching, configurable owned-file retention, telemetry, persistence, and shutdown flush are active |
| TD-013 | Add output error counters for ViGEm backend. | Medium | Low | Small | Output diagnostics service | Completed; connection, report-submission, and cleanup failures feed plugin snapshots, telemetry, stage warnings, and Output Monitor |
| TD-014 | Add formal plugin discovery/loading model. | Medium | High | Large | OutputAction/output manager architecture | Completed foundation in Chapter 15 |
| TD-015 | Add keyboard output backend. | Low | Medium | Medium | OutputAction/output plugin architecture | Completed in Chapter 10; mouse remains future work |
| TD-016 | Add recording/playback of runtime signals. | Medium | Medium | Large | RuntimeSignal model, storage format | Completed foundation in Chapter 14 |
| TD-017 | First-run ViGEmBus prompt blocks app initialization and automated smoke tests until user chooses. | Medium | Low | Small | Developer/test setting, startup service abstraction | Completed in Chapter 22; startup is non-blocking and Safe Mode suppresses the wizard |
| TD-018 | Hat, encoder, and switch visualizations are basic compared with axes/buttons. | Low | Low | Medium | UI diagnostics chapter | Completed; 3x3 hat direction, encoder pulse/direction, and switch position visuals retain Learn Mode and cached diagnostics |
| TD-019 | Driver install service duplicates ViGEmBus detection logic also present in output service. | Medium | Low | Small | Driver status service abstraction | Completed in Chapter 22 through `IVirtualGamepadDriverService` |
| TD-020 | Persist the remaining driver-prompt and advanced developer settings; Auto Save and recents now persist separately. | Medium | Low | Small | Settings service foundation | Completed in Chapter 22 settings schema v3 |
| TD-021 | Expand Chapter 3 signal publication into a formal typed event bus for profile, output, diagnostics, and plugin messages. | High | Medium | Medium | RuntimeSignal publication | Completed; one ordered, fault-isolated singleton publishes all five message families and exposes delivery counters |
| TD-022 | Move mapping execution and output scheduling off the UI dispatcher into an explicit runtime scheduler. | High | Medium | Medium | Event bus or runtime coordinator | Completed foundation in Chapter 12 |
| TD-023 | Add Signal Flow Inspector UI over `RuntimeStageDiagnostic` snapshots. | Medium | Low | Medium | Stage diagnostics foundation | Completed in Chapter 5 |
| TD-024 | Add configurable filtering algorithms to the active descriptor-based RuntimeSignal transform chain. | Medium | Low | Medium | Filter configuration/schema design | Completed; Moving Average, Exponential Smoothing, and Median Filter have centralized defaults/ranges, typed Transform Editor controls, isolated runtime state, validation, diagnostics, and deterministic regressions |
| TD-025 | Replace the live XboxState mapping boundary with signal-native OutputAction consumption; compatibility reducer retained for tests/backend state. | High | Medium | Medium | Output manager/plugin chapter | Completed foundation in Chapter 7 |
| TD-026 | Implement and hardware-validate a DirectInput provider if it adds devices or semantics unavailable through HID. | Medium | Medium | Large | Provider selection and DirectInput dependency | Deferred |
| TD-027 | Replace discovery polling with native Windows device notifications while preserving polling fallback. | Medium | Low | Medium | Input provider lifecycle | Completed; Input owns a tested message-only Windows HID notification source, CompositeInputProvider coalesces native wakeups, retains a 30-second safety poll, and automatically returns to two-second polling when registration is unavailable |
| TD-028 | Retrieve and persist Windows Container IDs for stronger device reconnection. | High | Low | Medium | SetupAPI property access | Completed; HID interface enumeration reads and validates `DEVPKEY_Device_ContainerId`, profiles backfill/persist it additively, and Stable IDs remain unchanged |
| TD-029 | Retire legacy `CompositeInputCatalog` and `CompositeInputEventSource` after compatibility callers migrate. | Low | Low | Small | Provider adapter adoption | Completed; both unreferenced aggregate classes were removed while active provider-specific compatibility contracts remain private to `InputProviderAdapter` |
| TD-030 | Add profile dirty tracking so Auto Save skips unchanged intervals and revisions. | Low | Low | Small | Profile coordinator extraction | Completed; content fingerprints cover nested configuration and accepted baselines follow every persisted workflow |
| TD-031 | Execute transform descriptors directly and retire remaining legacy behavior settings in a future migration. | High | Medium | Large | Transform engine/output plugin work | Completed runtime cutover in schema v8; descriptors are authoritative and legacy fields remain projections pending separately reviewed retirement |
| TD-032 | Add a versioned branching graph schema, persisted workspace positions, direct port editing, logic/plugin nodes, and large-graph virtualization while retaining linear mappings. | Medium | High | Large | Node Editor linear foundation and runtime graph execution design | Completed in profile schema v9/workspace schema v3 with bounded DAG runtime, typed ports, deterministic logic, registered internal extension point, graph-safe duplication/comparison, diagnostics, and focused tests |
| TD-033 | Add signed script packages, explicit permissions, bounded script resources, editor/diagnostic tooling, and a fail-closed isolation policy before accepting untrusted scripts. | High | High | Large | Chapter 20 Script API and Lua foundation | Completed trusted-code foundation; ECDSA packages, local publisher trust, per-operation permissions, source/queue/command/instruction/string/allocation quotas, Script Workbench, and isolation metadata are active; untrusted scripts remain blocked |
| TD-034 | Add vendor profile importers, signed profile packages, screenshots, and optional online community repository services. | Medium | Medium | Large | Chapter 21 local provider/package foundation | Completed evidence-backed local scope; signed bounded bundles, offline publisher trust, screenshots, and conservative T.A.R.G.E.T. MapKey conversion are active; online/community services are explicitly excluded |
| TD-035 | Sign the application and installer, publish checksums, and validate install, retained-build upgrade, rollback, repair, and uninstall on clean supported Windows machines. | High | High | Medium | Chapter 22 installer and deployment foundation | Completed release tooling; certificate-backed signing and release publication were completed without storing credentials in source control, while disposable clean-machine evidence remains an external release gate |
| TD-036 | Separate input-only monitoring from mapping/output activation so Device Inspector, Mapping Editor, Learn Mode, and Axis Curves can show live hardware while the virtual Xbox controller remains disconnected. | High | Medium | Medium | Input provider lifecycle and runtime coordinator extraction | Completed in the Easy input/output milestone; input monitoring starts independently and output activation remains mapping-owned |
| TD-037 | Add a Mouse Output plugin with relative movement, buttons, and wheel actions, including hat-to-pointer mapping and clean output reset. | Medium | Medium | Large | Output plugin SDK and mapping/output action extensions | Completed in the Easy input/output milestone; scheduled movement, buttons, wheel actions, visual authoring, diagnostics, and reset are covered |
| TD-038 | Add visual macro authoring and a guided Analog PWM mapping workflow. | High | Low | Medium | Macro and Mapping Editors plus Core validation | Completed; Visual Macro Editor and guided PWM authoring delivered |
| TD-039 | Add a keyboard assignment visualizer that highlights keys used by the active profile, supports direct selection, and shows mapping details on hover. | Medium | Low | Medium | Mapping Editor keyboard mode and conflict analysis | Completed in the Easy input/output milestone; status reconciled after implementation audit |
| TD-040 | Remove remaining App references to Input/Output implementation namespaces outside `App.xaml.cs`, covering keyboard capture and Test Runner composition. | Medium | Medium | Small | Published Core metadata and composition-root values | Completed; architecture baseline now has 0 `ARCH008` warnings |
| TD-041 | Move remaining service construction flagged outside the composition root to injected interfaces or documented factories as DI adoption proceeds. | Medium | Low | Medium | Playback/capture interfaces and benchmark factory | Completed; architecture baseline has 0 `ARCH007` findings |
| TD-042 | Review public services flagged without direct test references and add focused coverage or a documented analyzer exemption where indirect integration coverage is authoritative. | Medium | Medium | Medium | Runtime-store and non-destructive Windows boundary tests | Completed; architecture baseline has 0 `ARCH006` findings |
| TD-043 | Design optional remote AI providers, explicit payload preview/consent/redaction, secure credentials, and a typed proposal-review-apply workflow before any generated change can modify configuration. | Medium | High | Large | Chapter 24 immutable evidence and local advisor boundary | Deferred to Version 4.x |
| TD-044 | Define external plugin package trust, signature, permission, isolation, API compatibility, update, and recovery policy before loading third-party assemblies. | High | High | Large | Internal Plugin SDK foundation and formal `external-plugin-sdk` review gate | Deferred to Version 2.x |
| TD-045 | Implement a separately monitored script process host with authenticated IPC, strict resident-memory/CPU quotas, crash cleanup, and interactive breakpoints/step debugging before enabling untrusted packages. | High | High | Large | TD-033 trusted scripting foundation and formal scripting promotion review | Deferred to Version 3.x; current in-process policy fails closed |
| TD-046 | Add WinWing SimAppPro, Logitech, Virpil, or VKB profile converters only after official interchange schemas or representative user-approved fixtures support deterministic conversion tests. | Medium | Medium | Large | IProfileImporter registry and compatibility preview | Deferred pending format evidence; generic HID runtime support is unaffected |
| TD-047 | Add vJoy output and optionally bundle a vJoy driver only after selecting a maintained, signed upstream and completing supported-Windows driver acceptance. | Medium | High | Large | Explicit vJoy output use case, output-plugin implementation, driver/licensing review, clean-machine matrix | Deferred pending maintained driver evidence; existing vJoy input discovery remains supported and ViGEm remains the Xbox output dependency |

## Status Interpretation

`Completed` entries are retained as engineering history. `Deferred` entries describe explicitly excluded future scope and do not count as active debt for the current release. Any entry with another status is active and must be surfaced by Project Health.
